backend:
  phases:
    build:
      commands:
         - chmod u+x ./myamplifypush.sh
         - ./myamplifypush.sh
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*